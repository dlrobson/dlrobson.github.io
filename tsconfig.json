{
  // SvelteKit auto-generates this file on first `vite dev` or `vite build`.
  // Run `npm run dev` once to populate .svelte-kit/tsconfig.json.
  "extends": "./.svelte-kit/tsconfig.json",
  "compilerOptions": {
    "skipLibCheck": true,
    "strict": true,
    "moduleResolution": "bundler",
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  }
}